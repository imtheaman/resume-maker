import { createSlice } from "@reduxjs/toolkit";
const educationsSlice = createSlice({
  name: "educations",
  initialState: {},
  reducers: {},
});

const { reducer: educationsReducer, actions } = educationsSlice;
export const {} = actions;
export default educationsReducer;
