import SectionHeading from "./SectionHeading";

const Organization = () => {
  return (
    <div>
      <SectionHeading heading="Organizations" placeholder="Organizations" />
    </div>
  );
};

export default Organization;
