import SectionHeading from "./SectionHeading";

const Publication = () => {
  return (
    <div>
      <SectionHeading heading="Publications" placeholder="Publications" />
    </div>
  );
};

export default Publication;
